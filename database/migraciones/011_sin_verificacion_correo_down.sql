-- Reversión de 011: no hay forma de saber qué cuentas habían verificado de
-- verdad su correo antes; se deja el flag como está (TRUE). Solo se documenta
-- que para volver a exigir verificación hay que restaurar el envío en
-- auth.controller.js (registro y reenviar-verificacion).
SELECT 1;
